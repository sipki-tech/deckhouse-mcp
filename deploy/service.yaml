apiVersion: v1
kind: Service
metadata:
  name: deckhouse-mcp
  namespace: d8-system
  labels:
    app: deckhouse-mcp
spec:
  type: ClusterIP
  selector:
    app: deckhouse-mcp
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
