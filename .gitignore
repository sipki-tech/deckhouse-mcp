# Agent artifacts
.agents/
.spec/
AGENTS.md
skills-lock.json
mcp-handlers-full.md

# Binary (compiled in repo root only — keep cmd/deckhouse-mcp source tree visible)
/deckhouse-mcp
kind

# IDE
.idea/
.vscode/
*.swp

# Test artifacts
tests/integration/.port-forward.pid

# OS
.DS_Store
